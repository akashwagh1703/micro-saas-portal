# Required for production builds (vite.config.js fails if missing in production mode)
VITE_API_URL=https://api.autowave.playltp.in/api

# Optional — only if app is served from a subpath (e.g. /micro-saas/)
# VITE_BASE_PATH=/
